import { PrimeNumberDirective } from './prime-number.directive';

describe('PrimeNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new PrimeNumberDirective();
    expect(directive).toBeTruthy();
  });
});
